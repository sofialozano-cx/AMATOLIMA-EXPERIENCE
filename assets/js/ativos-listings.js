(()=>{const cards=[...document.querySelectorAll('.ativo-card-reveal')];const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}})},{threshold:.12,rootMargin:'0px 0px -6% 0px'});cards.forEach((card,i)=>{card.style.transitionDelay=`${(i%3)*90}ms`;card.style.cursor='pointer';card.setAttribute('role','link');card.setAttribute('tabindex','0');const open=()=>location.href=`ativo.html?id=${i+1}`;card.addEventListener('click',e=>{if(e.target.closest('.ativo-card__arrow')||e.target.closest('.ativo-card__slides'))return;open()});card.addEventListener('keydown',e=>{if(e.key==='Enter')open()});observer.observe(card)});document.querySelectorAll('.ativo-card').forEach(card=>{const slides=card.querySelector('.ativo-card__slides');const arrow=card.querySelector('.ativo-card__arrow');const dots=[...card.querySelectorAll('.ativo-card__dots i')];if(!slides||!arrow)return;let index=0;const update=()=>dots.forEach((dot,i)=>dot.classList.toggle('is-active',i===index));arrow.addEventListener('click',e=>{e.stopPropagation();const count=slides.children.length;index=(index+1)%count;slides.scrollTo({left:slides.clientWidth*index,behavior:'smooth'});update()});slides.addEventListener('click',e=>e.stopPropagation());slides.addEventListener('scroll',()=>{const next=Math.round(slides.scrollLeft/slides.clientWidth);if(next!==index){index=next;update()}},{passive:true});update()})})();