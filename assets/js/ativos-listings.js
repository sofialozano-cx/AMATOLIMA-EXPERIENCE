(()=>{const cards=[...document.querySelectorAll('.ativo-card-reveal')];const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){requestAnimationFrame(()=>entry.target.classList.add('is-visible'));observer.unobserve(entry.target)}})},{threshold:.01,rootMargin:'0px 0px 18% 0px'});cards.forEach((card,i)=>{card.style.transitionDelay=`${(i%3)*35}ms`;card.style.cursor='pointer';card.setAttribute('role','link');card.setAttribute('tabindex','0');card.setAttribute('aria-label',`Abrir ${card.querySelector('h3')?.textContent||'ativo'}`);const open=()=>location.href=`ativo.html?id=${i+1}`;card.addEventListener('click',e=>{if(e.target.closest('.ativo-card__arrow'))return;open()});card.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();open()}});observer.observe(card)});document.querySelectorAll('.ativo-card').forEach(card=>{const slides=card.querySelector('.ativo-card__slides');const arrow=card.querySelector('.ativo-card__arrow');const dots=[...card.querySelectorAll('.ativo-card__dots i')];if(!slides||!arrow)return;let index=0;const update=()=>dots.forEach((dot,i)=>dot.classList.toggle('is-active',i===index));arrow.addEventListener('click',e=>{e.stopPropagation();const count=slides.children.length;index=(index+1)%count;slides.scrollTo({left:slides.clientWidth*index,behavior:'smooth'});update()});let ticking=false;slides.addEventListener('scroll',()=>{if(ticking)return;ticking=true;requestAnimationFrame(()=>{const next=Math.round(slides.scrollLeft/slides.clientWidth);if(next!==index){index=next;update()}ticking=false})},{passive:true});update()})})();