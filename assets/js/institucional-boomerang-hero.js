(()=>{const root=document.querySelector('[data-boomerang-hero]');if(!root)return;const video=root.querySelector('video'),canvas=root.querySelector('canvas');if(!video||!canvas)return;const ctx=canvas.getContext('2d');if(!ctx)return;const frames=[];let lastTime=-1,capturing=true,playing=false;const capture=()=>{if(!capturing||video.ended)return;const t=video.currentTime;if(video.videoWidth&&t!==lastTime){lastTime=t;const w=Math.min(960,video.videoWidth),h=Math.round(w*video.videoHeight/video.videoWidth),c=document.createElement('canvas');c.width=w;c.height=h;const x=c.getContext('2d');x.drawImage(video,0,0,w,h);frames.push(c)}if('requestVideoFrameCallback'in video)video.requestVideoFrameCallback(capture);else requestAnimationFrame(capture)};const start=()=>{video.play().catch(()=>{});if('requestVideoFrameCallback'in video)video.requestVideoFrameCallback(capture);else requestAnimationFrame(capture)};video.addEventListener('loadeddata',start,{once:true});video.addEventListener('ended',()=>{capturing=false;if(frames.length<2)return;canvas.width=frames[0].width;canvas.height=frames[0].height;video.style.display='none';canvas.style.display='block';let i=0,dir=1,last=0;playing=true;const loop=ts=>{if(!playing)return;if(ts-last>=1000/30){last=ts;ctx.drawImage(frames[i],0,0,canvas.width,canvas.height);i+=dir;if(i>=frames.length-1){i=frames.length-1;dir=-1}else if(i<=0){i=0;dir=1}}requestAnimationFrame(loop)};requestAnimationFrame(loop)});if(video.readyState>=2)start()})();