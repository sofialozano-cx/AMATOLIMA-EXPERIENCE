(()=>{const root=document.querySelector('.viver-scroll'),panel=document.querySelector('.viver-panel'),gallery=document.querySelector('.viver-gallery'),overlay=document.querySelector('.viver-overlay'),view=document.querySelector('.viver-view'),footer=document.querySelector('.viver-footer'),info=document.querySelector('.viver-info'),video=document.querySelector('.viver-video video'),cards=[...document.querySelectorAll('.viver-card:not(.is-empty)')],cursor=document.querySelector('.viver-cursor');if(!root||!panel||!gallery)return;let raf=0,maxScroll=0;const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));const buildHeight=()=>{maxScroll=Math.max(0,gallery.scrollHeight-innerHeight);root.style.height=`${innerHeight+maxScroll+innerHeight*2}px`};if(video){video.muted=true;video.playsInline=true;video.loop=true;video.autoplay=true;const start=()=>video.play().catch(()=>{});if(video.readyState>=2)start();else video.addEventListener('canplay',start,{once:true})}const render=()=>{const y=scrollY,vh=innerHeight;if(y<=vh){panel.style.transform=`translateY(${vh-y}px)`;gallery.style.transform='translateY(0)'}else{panel.style.transform='translateY(0)';gallery.style.transform=`translateY(${-Math.min(y-vh,maxScroll)}px)`}cards.forEach(card=>{const r=card.getBoundingClientRect(),enter=Math.min(1,(vh-r.top)/(vh*.6)),exit=Math.min(1,r.bottom/(vh*.4)),s=r.bottom<=0||r.top>=vh?0:clamp(Math.min(enter,exit),0,1);card.style.transform=`scale(${s})`});const outro=clamp((y-vh-maxScroll)/(vh-100),0,1);overlay.style.opacity=outro;view.style.transform=`scale(${outro})`;footer.style.opacity=outro;if(info)info.style.transform='translateY(0)';raf=requestAnimationFrame(render)};addEventListener('mousemove',e=>{if(cursor){cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'}});addEventListener('resize',buildHeight);buildHeight();raf=requestAnimationFrame(render)})();